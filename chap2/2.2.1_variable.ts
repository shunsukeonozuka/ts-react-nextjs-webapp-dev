function calc(isSum: boolean) {
    let a = 100;
    if (isSum) {
        let b = a + 1;
        return b;
    }
    // return b;
    // >名前 'b' が見つかりません。
}

// const num: number = 100;
// num = 200;
// >定数であるため、'num' に代入することはできません。
